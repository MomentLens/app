// expo-env.d.ts carries this same reference, but it is gitignored and only written by
// `expo start`. Without this tracked copy, a fresh clone or CI typechecks with no
// declarations for CSS imports and the other globals Expo provides.
/// <reference types="expo/types" />
