import AppTabs from '@/components/app-tabs';

// The Global shell: Events and Profile. Scan joins them with the check-in slice (spec §2.5.1).
export default function TabsLayout() {
  return <AppTabs />;
}
