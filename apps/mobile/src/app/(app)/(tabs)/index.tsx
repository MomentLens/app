import { EventsScreen } from '@/features/events/events-screen';

// The Events tab, the Global shell's default landing (spec §2.5.1).
export default function EventsTab() {
  return <EventsScreen />;
}
